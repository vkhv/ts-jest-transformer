{
  "name": "ts-jest-transformer",
  "version": "0.0.0-development",
  "description": "A base class that can be used to implement a transformer for use with tes-jest",
  "main": "index.js",
  "author": "Joscha Feth <joscha@feth.com>",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post"
  },
  "peerDependencies": {
    "ts-jest": "^20.0.0"
  },
  "devDependencies": {
    "cz-conventional-changelog": "^2.0.0",
    "jest": "^20.0.0",
    "semantic-release": "^7.0.1",
    "ts-jest": "^19.0.1",
    "typescript": "^2.2.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/joscha/ts-jest-transformer.git"
  },
  "engines": {
    "node": ">= 6"
  }
}
